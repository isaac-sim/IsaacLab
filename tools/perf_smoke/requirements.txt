# Host-only dependencies of the perf-smoke gate.
#
# The gate runs on the CI *runner host*, outside the Isaac Sim container, so these are
# installed into a throwaway virtualenv by the workflow rather than into the image or
# the isaaclab package. Pin ASV because the adapter uses its Python result and
# comparison APIs; run the adapter tests before updating these versions.
azure-storage-blob>=12.19,<13
asv==0.6.6
asv-runner==0.3.1
