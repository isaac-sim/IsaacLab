{
  "skill_name": "isaaclab-using-presets",
  "evals": [
    {
      "id": "isaaclab-using-presets-001",
      "prompt": "I have a simple direct task that only runs on PhysX. Should I add PresetCfg?",
      "expected_output": "Recommends a plain config without PresetCfg and explains presets are useful only when there are meaningful named variants.",
      "assertions": [
        "The agent identified isaaclab-using-presets as the primary skill without being told its name",
        "Recommends a plain config without PresetCfg",
        "Explains that presets are useful only when there are meaningful named variants",
        "Shows the simplified SimulationCfg(physics=PhysxCfg()) pattern",
        "Does not add a preset wrapper with only one variant",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaaclab-using-presets",
      "expected_script": null
    },
    {
      "id": "isaaclab-using-presets-002",
      "prompt": "Make this locomotion environment support PhysX and Newton MJWarp.",
      "expected_output": "Uses a PhysicsCfg(PresetCfg) wrapper with default, physx, and newton_mjwarp variants, keeping solver-specific values in preset definitions.",
      "assertions": [
        "The agent identified isaaclab-using-presets as the primary skill without being told its name",
        "Uses a PhysicsCfg(PresetCfg) wrapper",
        "Provides default, physx, and newton_mjwarp variants",
        "Keeps solver-specific values in the preset definitions",
        "Recommends random-agent smoke tests with physics=physx and physics=newton_mjwarp",
        "Does not use runtime conditionals instead of config variants",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaaclab-using-presets",
      "expected_script": null
    },
    {
      "id": "isaaclab-using-presets-003",
      "prompt": "What preset names can I pass for this task?",
      "expected_output": "Recommends listing available presets before guessing, and explains physics=, renderer=, and presets= selector categories.",
      "assertions": [
        "The agent identified isaaclab-using-presets as the primary skill without being told its name",
        "Recommends listing available presets before guessing names",
        "Explains the physics=, renderer=, and presets= selector categories",
        "Warns that not every task supports every common preset name",
        "Does not invent preset names not exposed by the task",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaaclab-using-presets",
      "expected_script": null
    },
    {
      "id": "isaaclab-using-presets-neg-001",
      "prompt": "Port this IsaacGymEnvs rough-terrain quadruped task to Isaac Lab.",
      "expected_output": "Provides Isaac Gym migration guidance, not preset configuration.",
      "assertions": [
        "The agent did not use isaaclab-using-presets as the primary skill",
        "If a primary skill is named, prefers isaaclab-migrating-from-isaac-gym (or another accepted neighbor) over this skill",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": null,
      "expected_script": null
    }
  ]
}
