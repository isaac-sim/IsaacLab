Added
^^^^^

* Added :func:`~isaaclab_tasks.utils.cfg_ref` for lazy preset references to
  optional backend config classes.

Fixed
^^^^^

* Fixed Cartpole kitless training imports so Newton presets can run without
  installing PhysX or OVPhysX backend packages.
