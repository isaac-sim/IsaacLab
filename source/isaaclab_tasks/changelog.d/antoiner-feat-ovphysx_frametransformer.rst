Changed
^^^^^^^

* Wired :class:`~isaaclab_ovphysx.physics.OvPhysxCfg` into the
  ``Isaac-Franka-Cabinet-Direct-v0`` preset so the env can run on the
  OVPhysX backend via ``--physics=ovphysx``. Same pattern other direct
  tasks (cartpole, ant, humanoid, allegro_hand) already use.
