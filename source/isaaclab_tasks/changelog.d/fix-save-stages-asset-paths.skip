Fixed
^^^^^

* Fixed :func:`~rendering_test_utils.maybe_save_stage` to export via ``Usd.Stage.Flatten()``
  when ``ISAAC_LAB_SAVE_STAGES`` is set, so asset paths in the saved USDA are resolved
  relative to the output file instead of being left as bare Nucleus content-root references.
