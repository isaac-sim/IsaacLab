Test-only change: dropped the OpenUSD work-thread limit that the kit-less rendering
fixture and the environment smoke tests applied to work around a race in older
OpenUSD releases. No user-facing behavior changes, so no changelog entry is warranted.
