# G1 deployment policy
G1 task env prepared by IsaacLab uses G1 asset whose joints configurations are different from actual hardware. 
We define another G1 task so that the learned policy can be directly executed on the hardware. 