Changed
^^^^^^^

* **Breaking:** Pinned the ``ovrtx`` optional extra for :mod:`isaaclab_ov` to
  ``>=0.3.0,<0.4.0`` (previously ``>=0.2.0,<0.3.0``). Install ``ovrtx`` 0.3.x
  when using the ``[ovrtx]`` extra.
