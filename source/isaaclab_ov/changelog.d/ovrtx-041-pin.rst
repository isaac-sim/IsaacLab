Changed
^^^^^^^

* Updated the optional OVRTX runtime dependency to ``ovrtx==0.4.1.364272``,
  resolved from the internal ``ct-omniverse-pypi`` index. Reinstall the OVRTX
  extra with ``uv run --extra ovrtx`` (or ``./isaaclab.sh -i 'ov[ovrtx]'``) to
  pick up that build.
