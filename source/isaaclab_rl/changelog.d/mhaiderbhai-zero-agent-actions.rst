Fixed
^^^^^

* Fixed the zero agent to defer zero-action handling to manager-based action terms, support composite and multi-agent
  action spaces, and reject invalid task configurations before launching the simulator.
