Added
^^^^^

* Added the opt-in training ``--export_deployment_usd`` flag to save environment zero after physics and asset initialization, before startup randomization and training reset. Reused the initialized scene and recorded incremental export timings in the run log directory.
