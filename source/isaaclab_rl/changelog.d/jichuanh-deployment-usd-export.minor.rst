Added
^^^^^

* Added the opt-in training ``--export_deployment_usd`` flag to save environment zero after normal task construction and one-time prestartup/startup events, before training reset. Reused the initialized scene and recorded incremental export timings in the run log directory.
