Added
^^^^^

* Added Isaac Lab CLI ``train`` and ``play`` aliases for launching the unified
  reinforcement learning scripts.
* Added ``uv run train`` and ``uv run play`` source checkout entrypoints with
  default RSL-RL, tasks, and Newton dependencies, plus optional dependency extras
  for OVRTX and OVPhysX workflows.
