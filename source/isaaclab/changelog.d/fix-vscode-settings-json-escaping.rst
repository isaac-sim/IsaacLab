Fixed
^^^^^

* Fixed the VS Code settings generated by ``python -m isaaclab --generate-vscode-settings``
  producing invalid JSON on Windows. The interpreter path and extension paths were interpolated
  into the template verbatim, so backslashes in paths like ``C:\Users\...`` became invalid JSON
  escape sequences. Paths are now serialized with :func:`json.dumps`.
