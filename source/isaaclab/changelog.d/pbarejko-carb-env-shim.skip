Fixed
^^^^^

* Fixed intermittent ``getenv`` SIGSEGVs in Docker CI by preloading the
  Carbonite ``libcarb.env.shim.so`` via ``/etc/ld.so.preload`` instead of
  building a custom env-function shim at image-build time.
