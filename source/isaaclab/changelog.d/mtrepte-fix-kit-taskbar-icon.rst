Fixed
^^^^^

* Fixed Linux docks showing a generic icon for the Kit and Newton visualizer windows. On Linux
  graphical sessions, ``isaaclab --install`` now writes hidden desktop entries for them to
  ``$XDG_DATA_HOME/applications`` (default ``~/.local/share/applications``).
