Fixed
^^^^^

* Fixed headless Kit viewport capture with Fabric-backed physics by using a visualizer-owned camera
  with the configured pose instead of Kit's stale default perspective-camera transform.
